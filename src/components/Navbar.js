import { Box, Container, Typography } from "@mui/material";

function Navbar() {
  return (
    <Container maxWidth={false} sx={{backgroundColor: "#907FA4", height: 100}}>
      <Typography textAlign="center">CHATBOT</Typography>
    </Container>
  );
}

export default Navbar;
